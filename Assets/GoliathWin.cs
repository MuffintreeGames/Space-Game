using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoliathWin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("The goliath wins!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
